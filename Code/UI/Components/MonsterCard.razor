@namespace Beastborne.UI.Components
@using System.Linq;
@using Sandbox;
@using Sandbox.UI;
@using Beastborne.Core;
@using Beastborne.Data;
@using Beastborne.Systems;
@inherits Panel

@if (IsHorizontal)
{
	<!-- Horizontal Card: Sprite on left, info column on right.
	     Used in compact contexts like fusion parent slots where the
	     vertical mini layout eats too much height. -->
	<div class="monster-card horizontal @(IsSelected ? "selected" : "") @GetRarityClass()"
		 onclick=@OnCardClicked onmouseover=@OnMouseEnter onmouseout=@OnMouseLeave>
		<!-- Left: Sprite -->
		<div class="horiz-sprite-wrap">
			@if (HasSprite())
			{
				<div class="horiz-sprite monster-icon" style="background-image: url(@GetCurrentFrame())"></div>
			}
			else
			{
				<div class="horiz-sprite-placeholder">?</div>
			}
		</div>

		<!-- Right: Info column -->
		<div class="horiz-info">
			<div class="horiz-name-row">
				<img class="horiz-element-icon" src="ui/icons/elements/background/@(GetElementClass()).svg" />
				@if (HasSecondaryElement())
				{
					<img class="horiz-element-icon secondary" src="ui/icons/elements/background/@(GetSecondaryElementClass()).svg" />
				}
				<span class="horiz-name">@Monster.Nickname</span>
				@if (Monster.IsFavorite)
				{
					<iconify icon="lucide:star" Size=@(12) Color="#fbbf24" />
				}
			</div>
			<div class="horiz-stat-row">
				<div class="horiz-stat lv">
					<span class="hs-label">LV</span>
					<span class="hs-val">@Monster.Level</span>
				</div>
				<div class="horiz-stat pwr">
					<span class="hs-label">PWR</span>
					<span class="hs-val">@Monster.PowerRating.ToString("N0")</span>
				</div>
				@if (Monster?.Genetics != null)
				{
					<div class="horiz-stat genes @GetGeneticsClass()">
						<iconify icon="lucide:dna" Size=@(11) Color="#c4b5fd" />
						<span class="hs-val">@Monster.Genetics.TotalValue</span>
					</div>
				}
			</div>
			<!-- Quality micro-bar -->
			<div class="horiz-quality-bar-bg">
				<div class="horiz-quality-bar-fill @GetGeneticsClass()" style="width: @(((Monster?.Genetics?.TotalValue ?? 0) / 180f * 100f).ToString("F0"))%"></div>
			</div>
		</div>

	</div>
}
else if (IsMini)
{
	<!-- Mini Card: Sprite + overlays -->
	<div class="monster-card mini @(IsSelected ? "selected" : "") @(ShowGenes ? "show-genes" : "")"
		 onclick=@OnCardClicked onmouseover=@OnMouseEnter onmouseout=@OnMouseLeave>
		<!-- Sprite. Aspect ratio preserved via `background-size: contain`
		     on an explicitly-sized inner div. NOT transform — transform
		     scaling smooths pixel art in s&box even with image-rendering:
		     pixelated. NOT bg-size: <Npx> <Npx> — that forces a square
		     box and squashes wide/tall source canvases (Aurael, Seraphiel,
		     etc.). The inner's width/height = 150 × MiniScale square, and
		     contain fits the source preserving aspect. The outer clips
		     overflow at the card-sprite area. Per-species MiniOffsetX/Y
		     shift the inner; OverrideArtSpeciesId substitutes a different
		     species so framing can be tuned for any beast (dev tool). -->
		@if (HasSprite() || HasOverrideSprite())
		{
			var _miniArtId = !string.IsNullOrEmpty(OverrideArtSpeciesId) ? OverrideArtSpeciesId : Monster?.SpeciesId;
			var _miniSpecies = MonsterManager.Instance?.GetSpecies(_miniArtId);
			var _miniCi = System.Globalization.CultureInfo.InvariantCulture;
			var _miniScale = _miniSpecies?.MiniScale ?? 1f;
			var _miniBoxPx = 150f * _miniScale;
			var _miniLeft = (150f - _miniBoxPx) * 0.5f + (_miniSpecies?.MiniOffsetX ?? 0f);
			var _miniTop = (150f - _miniBoxPx) * 0.5f + (_miniSpecies?.MiniOffsetY ?? 0f);
			var _miniBoxStr = _miniBoxPx.ToString("F1", _miniCi);
			var _miniLeftStr = _miniLeft.ToString("F1", _miniCi);
			var _miniTopStr = _miniTop.ToString("F1", _miniCi);
			var _miniStyle = $"background-image: url({GetCurrentFrame()}); width: {_miniBoxStr}px; height: {_miniBoxStr}px; left: {_miniLeftStr}px; top: {_miniTopStr}px;";
			<div class="mini-icon">
				<div class="mini-icon-bg monster-icon" style="@_miniStyle"></div>
			</div>
			<!-- Bottom fade — beasts' legs/feet fade into the rarity bar
			     instead of butting against it, making the card read like
			     a portrait nameplate (legs cropped behind the name) rather
			     than a sticker on a square. -->
			<div class="mini-icon-fade"></div>
		}
		<!-- Top-left badge stack: LV pill + (optional) genes pill flow
		     side-by-side via flex so the genes pill auto-positions next
		     to the LV pill regardless of digit count (LV5 vs LV100). -->
		<div class="mini-badge-row">
			<div class="mini-lv-pill">
				<span class="lv-text">@($"LV{Monster.Level}")</span>
			</div>
			@if (ShowGenes && Monster?.Genetics != null)
			{
				<div class="mini-genes-pill @GetGeneticsClass()">
					<iconify icon="lucide:dna" Size=@(11) Color="#c4b5fd" />
					<span class="genes-text">@Monster.Genetics.TotalValue</span>
				</div>
			}
		</div>
		<!-- Top-right: Favorite -->
		@if (Monster.IsFavorite)
		{
			<div class="mini-fav"><iconify icon="lucide:star" Size=@(12) Color="#fbbf24" /></div>
		}
		<!-- Rarity edge line -->
		<div class="mini-rarity-edge @GetRarityClass()"></div>
		<!-- Bottom: Name bar with element icon(s). Dual-type beasts get
		     a second smaller icon stacked next to the primary. -->
		<div class="mini-name-bar">
			<img class="mini-name-el" src="ui/icons/elements/background/@(GetElementClass()).svg" />
			@if (HasSecondaryElement())
			{
				<img class="mini-name-el secondary" src="ui/icons/elements/background/@(GetSecondaryElementClass()).svg" />
			}
			<span class="mini-name">@(!string.IsNullOrEmpty(Monster.Nickname) ? Monster.Nickname : (MonsterManager.Instance?.GetSpecies(Monster.SpeciesId)?.Name ?? Monster.SpeciesId))</span>
		</div>
	</div>
}
else
{
	<div class="monster-card @(IsSelected ? "selected" : "") @(IsCompact ? "compact" : "") @GetRarityClass()"
		 onclick=@OnCardClicked
		 onmouseover=@OnMouseEnter
		 onmouseout=@OnMouseLeave>

		<!-- Header -->
		<div class="card-header">
			<span class="monster-level">LV @Monster.Level</span>
			@if (CanEvolve())
			{
				<span class="evolve-icon" title="Can Evolve">⬆</span>
			}
			<div class="rarity-badge" title="@GetRarityName()">
				<span class="rarity-label-text">@GetRarityShort()</span>
				<img class="rarity-pip-svg" src="ui/icons/rarity/@(GetRarityPipClass()).svg" />
			</div>
		</div>

		<!-- Monster Icon (Animated) -->
		<div class="monster-icon-container">
			@if (Monster.IsFavorite)
			{
				<span class="favorite-icon">★</span>
			}
			@if (HasSprite())
			{
				<div class="monster-icon" style="background-image: url(@GetCurrentFrame())"></div>
			}
			else
			{
				<div class="monster-icon-placeholder">?</div>
			}
		</div>

		<!-- Name -->
		<div class="monster-name-row">
			<div class="monster-name">@Monster.Nickname</div>
		</div>

		@if (!IsCompact)
		{
			<!-- Stats -->
			<div class="stats-container">
				<div class="stat-row">
					<span class="stat-label">HP</span>
					<div class="stat-bar-bg">
						<div class="stat-bar-fill hp" style="width: @(GetStatPercent(Monster.MaxHP, 1400))%"></div>
					</div>
					<span class="stat-value">@Monster.MaxHP</span>
				</div>
				<div class="stat-row">
					<span class="stat-label">ATK</span>
					<div class="stat-bar-bg">
						<div class="stat-bar-fill atk" style="width: @(GetStatPercent(Monster.ATK, 1400))%"></div>
					</div>
					<span class="stat-value">@Monster.ATK</span>
				</div>
				<div class="stat-row">
					<span class="stat-label">DEF</span>
					<div class="stat-bar-bg">
						<div class="stat-bar-fill def" style="width: @(GetStatPercent(Monster.DEF, 1400))%"></div>
					</div>
					<span class="stat-value">@Monster.DEF</span>
				</div>
				<div class="stat-row">
					<span class="stat-label">SpA</span>
					<div class="stat-bar-bg">
						<div class="stat-bar-fill spa" style="width: @(GetStatPercent(Monster.SpA, 1400))%"></div>
					</div>
					<span class="stat-value">@Monster.SpA</span>
				</div>
				<div class="stat-row">
					<span class="stat-label">SpD</span>
					<div class="stat-bar-bg">
						<div class="stat-bar-fill spd-def" style="width: @(GetStatPercent(Monster.SpD, 1400))%"></div>
					</div>
					<span class="stat-value">@Monster.SpD</span>
				</div>
				<div class="stat-row">
					<span class="stat-label">SPD</span>
					<div class="stat-bar-bg">
						<div class="stat-bar-fill spd" style="width: @(GetStatPercent(Monster.SPD, 1400))%"></div>
					</div>
					<span class="stat-value">@Monster.SPD</span>
				</div>
			</div>

			<!-- Genetics Quality -->
			@if (ShouldShowGenetics())
			{
				<div class="genetics-row">
					<span class="genetics-label">Genes:</span>
					<span class="genetics-value @GetGeneticsClass()">@Monster.Genetics.QualityRating</span>
				</div>
			}
		}

		<!-- Power Rating with Element -->
		@if (ShouldShowPowerRating())
		{
			<div class="power-rating">
				<span class="power-label">PWR</span>
				<span class="power-value">@Monster.PowerRating</span>
				<img class="element-badge-svg" src="ui/icons/elements/background/@(GetElementClass()).svg" title="@GetElementName()" />
				@if (HasSecondaryElement())
				{
					<img class="element-badge-svg secondary" src="ui/icons/elements/background/@(GetSecondaryElementClass()).svg" />
				}
			</div>
		}
	</div>
}

@code {
	// Localization helper
	private string L( string key ) => LocalizationManager.Get( key );
	private string L( string key, params object[] args ) => LocalizationManager.Get( key, args );

	public Monster Monster { get; set; }
	public bool IsSelected { get; set; }
	public bool IsCompact { get; set; }
	public bool IsMini { get; set; }
	public bool IsHorizontal { get; set; } // Horizontal variant: sprite left, info right (used in fusion parent slots)
	public bool IsInTeam { get; set; }
	public bool ShowGenes { get; set; } // Mini-card variant: shows the gene-total pill alongside LV (used in fusion mode)
	public bool UseStaticImage { get; set; } = false;
	public bool AnimateOnHover { get; set; } = false;
	public Action ClickAction { get; set; }
	public Action HoverAction { get; set; }
	public Action LeaveAction { get; set; }
	public bool ShouldAnimate { get; set; } = false;
	public int AnimationFrame { get; set; } = 0;

	// Dev-tool override (used by MonsterRosterPanel's beast-art editor):
	// when set, the mini variant renders this species' sprite + Mini
	// transforms instead of the actual Monster's species. Lets the dev
	// tune framing for species the player doesn't own. Everything else
	// (level, name, badges) keeps reading from the real Monster.
	public string OverrideArtSpeciesId { get; set; }

	// Dev-tool re-render trigger. Bumped by MonsterRosterPanel whenever
	// the editor mutates a species' MiniScale/Offset values so this
	// component re-renders and picks up the live species values. Without
	// this, Razor's prop-diff sees no change (we're mutating shared
	// MonsterSpecies fields, not props) and the inline transform stays
	// at whatever the last render captured.
	public int ArtVersion { get; set; } = 0;

	private Panel iconElement;
	private int lastRenderedFrame = -1;
	private bool wasShouldAnimate = false;

	protected override void OnAfterTreeRender( bool firstTime )
	{
		base.OnAfterTreeRender( firstTime );

		// Always find the icon element after render as it may have been recreated
		FindIconElement();
	}

	private void FindIconElement()
	{
		foreach ( var child in Descendants )
		{
			if ( child is Panel panel && panel.HasClass( "monster-icon" ) )
			{
				iconElement = panel;
				break;
			}
		}
	}

	public override void Tick()
	{
		base.Tick();

		// Handle animation state changes
		if ( ShouldAnimate != wasShouldAnimate )
		{
			wasShouldAnimate = ShouldAnimate;
			// Reset frame tracking so animation updates on next tick
			lastRenderedFrame = -1;
			// Re-find icon element in case it was recreated
			FindIconElement();

			if ( !ShouldAnimate && iconElement != null )
			{
				// Reset to static frame when animation stops. Honor the
				// editor override so the test species' static frame stays
				// rendered instead of reverting to the real Monster's art.
				var artId = !string.IsNullOrEmpty( OverrideArtSpeciesId ) ? OverrideArtSpeciesId : Monster?.SpeciesId;
				var species = MonsterManager.Instance?.GetSpecies( artId );
				if ( species?.AnimationFrames != null && species.AnimationFrames.Count > 0 )
				{
					iconElement.Style.SetBackgroundImage( species.AnimationFrames[0] );
				}
			}
		}

		// Determine if we should animate
		// - If AnimateOnHover is true, use the ShouldAnimate prop from parent
		// - If UseStaticImage is true and AnimateOnHover is false, never animate
		// - Otherwise, always animate
		bool shouldAnimate = AnimateOnHover ? ShouldAnimate : !UseStaticImage;

		if ( !shouldAnimate ) return;

		// Try to find the icon element if not found yet
		if ( iconElement == null )
		{
			FindIconElement();
			if ( iconElement == null ) return;
		}

		// Read directly from SpriteAnimator instead of relying on passed parameter
		int currentFrame = SpriteAnimator.GlobalFrame;

		// Update the icon's background image directly when frame changes.
		// Editor override → animate the test species' frames instead of
		// the real Monster's, so hovering an editing card doesn't snap
		// the sprite back to whatever beast actually owns the slot.
		if ( currentFrame != lastRenderedFrame )
		{
			lastRenderedFrame = currentFrame;
			var artId = !string.IsNullOrEmpty( OverrideArtSpeciesId ) ? OverrideArtSpeciesId : Monster?.SpeciesId;
			var species = MonsterManager.Instance?.GetSpecies( artId );
			if ( species?.AnimationFrames != null && species.AnimationFrames.Count > 0 )
			{
				int frameIndex = currentFrame % species.AnimationFrames.Count;
				var framePath = species.AnimationFrames[frameIndex];
				iconElement.Style.SetBackgroundImage( framePath );
			}
		}
	}

	private void OnCardClicked()
	{
		Log.Info($"MonsterCard clicked: {Monster?.Nickname ?? "null"}, ClickAction is {(ClickAction != null ? "SET" : "NULL")}");
		ClickAction?.Invoke();
	}

	private bool isInternallyHovered = false;

	private void OnMouseEnter()
	{
		// Prevent duplicate calls from child element bubbling
		if ( isInternallyHovered ) return;

		isInternallyHovered = true;
		HoverAction?.Invoke();
	}

	private void OnMouseLeave()
	{
		// Check if mouse is still within this panel (moved to child element)
		// HasHovered is true if mouse is over this panel or any descendant
		if ( HasHovered ) return;

		isInternallyHovered = false;
		LeaveAction?.Invoke();
	}

	private bool HasSprite()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		if ( species == null ) return false;

		// Check if has animation frames or icon path
		if ( species.AnimationFrames != null && species.AnimationFrames.Count > 0 )
			return true;
		if ( !string.IsNullOrEmpty( species.IconPath ) )
			return true;

		return false;
	}

	private bool HasOverrideSprite()
	{
		if ( string.IsNullOrEmpty( OverrideArtSpeciesId ) ) return false;
		var sp = MonsterManager.Instance?.GetSpecies( OverrideArtSpeciesId );
		if ( sp == null ) return false;
		if ( sp.AnimationFrames != null && sp.AnimationFrames.Count > 0 ) return true;
		if ( !string.IsNullOrEmpty( sp.IconPath ) ) return true;
		return false;
	}

	private string GetCurrentFrame()
	{
		// Dev override (beast-art editor): when set, render the override
		// species' sprite instead of Monster's. Lets the editor preview
		// framing for species the player doesn't own.
		var artSpeciesId = !string.IsNullOrEmpty( OverrideArtSpeciesId ) ? OverrideArtSpeciesId : Monster?.SpeciesId;
		var species = MonsterManager.Instance?.GetSpecies( artSpeciesId );
		if ( species == null )
			return "";

		// If using static image or animate-on-hover (and not currently animating), return first frame
		if ( UseStaticImage || (AnimateOnHover && !ShouldAnimate) )
		{
			if ( species.AnimationFrames != null && species.AnimationFrames.Count > 0 )
				return species.AnimationFrames[0];
			return species.IconPath ?? "";
		}

		// Check if species has animation frames defined
		if ( species.AnimationFrames != null && species.AnimationFrames.Count > 0 )
		{
			int frameIndex = SpriteAnimator.GlobalFrame % species.AnimationFrames.Count;
			return species.AnimationFrames[frameIndex];
		}

		// Fall back to static icon
		return species.IconPath ?? "";
	}

	private string GetRarityClass()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return $"rarity-{species?.BaseRarity.ToString().ToLower() ?? "common"}";
	}

	private string GetRarityPipClass()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.BaseRarity.ToString().ToLower() ?? "common";
	}

	private string GetRarityName()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.BaseRarity.ToString() ?? "Common";
	}

	private string GetRarityShort()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.BaseRarity switch
		{
			Rarity.Common => "CMN",
			Rarity.Uncommon => "UNC",
			Rarity.Rare => "RARE",
			Rarity.Epic => "EPIC",
			Rarity.Legendary => "LGND",
			Rarity.Mythic => "MYTH",
			_ => "CMN"
		};
	}

	private string GetRarityIcon()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.BaseRarity switch
		{
			Rarity.Common => "●",
			Rarity.Uncommon => "◆",
			Rarity.Rare => "◆",
			Rarity.Epic => "★",
			Rarity.Legendary => "★",
			Rarity.Mythic => "✦",
			_ => "●"
		};
	}

	private string GetElementClass()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.Element.ToString().ToLower() ?? "neutral";
	}

	// Returns the secondary element class for dual-type beasts, or
	// null if the species is single-type. Used by render variants
	// that show both element badges.
	private string GetSecondaryElementClass()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.SecondaryElement?.ToString().ToLower();
	}

	private bool HasSecondaryElement()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.SecondaryElement.HasValue == true;
	}

	private string GetElementName()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.Element.ToString() ?? "Neutral";
	}

	private string GetElementShort()
	{
		var species = MonsterManager.Instance?.GetSpecies( Monster?.SpeciesId );
		return species?.Element switch
		{
			ElementType.Fire => "FIR",
			ElementType.Water => "WTR",
			ElementType.Earth => "ERT",
			ElementType.Wind => "WND",
			ElementType.Electric => "ELC",
			ElementType.Ice => "ICE",
			ElementType.Nature => "NAT",
			ElementType.Metal => "MTL",
			ElementType.Shadow => "SHD",
			ElementType.Spirit => "SPR",
			_ => "NEU"
		};
	}

	private float GetStatPercent( int value, int max )
	{
		return Math.Min( 100, (float)value / max * 100 );
	}

	private string GetGeneticsClass()
	{
		return Monster?.Genetics?.QualityRating?.ToLower() switch
		{
			"perfect" => "perfect",
			"excellent" => "excellent",
			"great" => "great",
			"good" => "good",
			_ => "average"
		};
	}

	private bool CanEvolve()
	{
		if ( Monster == null ) return false;
		var species = MonsterManager.Instance?.GetSpecies( Monster.SpeciesId );
		if ( species == null ) return false;
		return Monster.CanEvolve( species );
	}

	// Settings helpers
	private bool ShouldShowGenetics() => SettingsManager.Instance?.Settings?.ShowGeneticsOnCards ?? false;
	private bool ShouldShowPowerRating() => SettingsManager.Instance?.Settings?.ShowPowerRatings ?? true;
}
