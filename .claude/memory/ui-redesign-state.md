---
name: ui-redesign-state
description: "2026-06 UI overhaul (Concept C menu, Persona transition, Wind→teal) + 2026-07 Fable 5 sweep prep: guiding-star refreshed as brief, bottom bar → phone launcher, element hues open to Fable 5."
metadata: 
  node_type: memory
  type: project
  originSessionId: d451fa20-5528-48d8-b237-b196569b2c27
---

**MORNING 2026-07-03 (user feedback round, →23d587a):** Beasts polish per user (18b5c42): element tag = LV/PWR badge peer (19px glyph, skewed); card el-tiles 24px; shadow disc CUT ("remove the element bar thats on the circle"); halo softened (was "solid plate"); rarity → species kicker color; quality+nature SIDE-BY-SIDE (explicit-height row). Beastbook refinement (23d587a, agent + my live fixes): rail uniform via TileArtK (old art-space offsets were the raggedness), undiscovered registry dossier w/ zone-aware hints, soft teal spotlight, pedestal bar cut, big element pills, **SPOILER GATE added** (portrait art-viewer leaked full art + NAME of undiscovered species — caught live; gated at OnPortraitClick + render), **identity-stack collapse fixed** (flex-wrap row stacked all rows at one Y; recipe = kill wrap + explicit row heights + STATE-KEYED wrapper heights idn-full/seen/min — new law recorded). Tour status: Beasts ✓ Beastbook ✓ both user-shaped; Shop/Quests/SkillTree/Online live-verified healthy, no further pass yet; Achievements/Daily/GiftInbox/Trading/Tutorial/showcases still capture-unverified (wave-4 static only).

**OVERNIGHT 2026-07-03 (user asleep, full license, →f8e34c2):** Stage v2 per user feedback (b1968ed): WHOLE sprite contain-fit at 400px (per-species DetailIcon crop transforms RETIRED — art editor DETAIL mode now vestigial), violet halo circle + soft shadow disc (tucked up for flyers), auto-margin vertical centering, facts 560px w/ 2-col compact stats + type scale-up, collection scroll-x clamped. Then SWEEP WAVE 4 (f8e34c2): the 10 never-swept live surfaces (Contract, ExpeditionResult, Achievements, Daily, GiftInbox, Trading, both Showcases, Tutorial, Credits) via 3 agents. I played a FULL LV1 expedition run by self-drive to verify: battle HUD pixel-identical (card-v2 scoped-selector shield HELD), contract offer/refusal/SUCCESS screens + victory popup all live at 76-138fps — side effect: caught the user a spare Twincoil (LV6, veteran) + 75g/60xp, spent ~340 ink on two failed attempts. Team picker picked-state (violet+green check) verified. BreedingPanel + CardCollectionPanel rendered NOWHERE = dead code, flagged for deletion (not swept). Battle drive protocol learned: ESC = ENGINE overlay (never send; Q is game-back), fight menu 1=Intimidate 4=Hurricane Slam, 2-enemy waves need a TARGET click, key-timing drifts into stray CONTRACT menus — use mouse clicks (FIGHT .818/.705, slam .7915/.807). NOT yet visually verified (compile-clean + static only): Trading, showcases, Tutorial, Credits, Achievements, Daily, GiftInbox — each agent left ranked capture risks in its report; check on next session.

**CENTER STAGE + DRIFT BACKGROUNDS ✅ (2026-07-02 late night, →53eed58):** Beasts page went twice more after the sweep: ground rework (61fb5b3: FilterBar resweep, slab tiers, diamonds dissolved, ONE ring both zones) then a FULL fresh-skeleton **Center Stage rebuild** (user-picked via option question; blueprint at `.claude/ui-knowledge/beasts-center-stage.md`): grid left 690px / 2.5× sprite stage center / 520px facts right; fusion altar ON the stage; multi-release dockbar; ring flies to dossier controls on MOUSE HOVER too (WrapDetailRing/FindHoveredRingHost, RingHostClasses). MonsterCard v2 (e789b9b: all modes, MiniSize param, scroll-clip purge, battle HUD shielded by element-scoped selector twins). **BbIconScroll** drift bgs (user concept — panel's own glyph drifting at the house angle) live on Beastbook/Beasts/shop/bag/quests/skills/online/leaderboards; Expedition SKIPPED (zone art IS its background). NEW LAWS in learnings: never rotate a tiled layer per frame (0fps); component-root position:absolute unreliable → zero-footprint in-flow root; padding-less flex wrapper under a scroll column collapses children's LAYOUT height (paint≠flow; dissolve the wrapper, min-height fixes nothing); ESC in-game opens the ENGINE overlay (use Q in drives); hotload lambda debris = dead onclicks + 9fps pages (restart law, again). All verified live by self-drive EXCEPT: team-picker picked-state, battle-HUD glance, bag/shop drift visuals, evolution FX at the 300px stage, altar at 1920 — **blocked because the editor game session ZOMBIED after MCP stop/start play** (frozen viewport, empty in-memory scene, stuck "Playing"; mainscene.scene on disk INTACT+git-clean — do NOT save scene from the wedged editor; plain editor restart fixes; classifier rightly blocked my process kill). WALK_DEBUG now false. ~248 commits local-only.

**FULL PANEL SWEEP ✅ COMPLETE (2026-07-02 night, →4d7b6db):** every in-game panel redone + verified live on a fresh session. Final board: infra (8af77e0), wave 1 (498d859), wave 2 (5bf286e), roster recomposition fix (a0aacfc — NEW LAW: razor `}` only terminates a block at HTML element depth zero; unclosed div = brace swallowed as text), wave 3 + player-tag retirement (4d7b6db — the PawPad chip is the ONLY screen furniture now, user call). Beasts stage-primary layout verified live (2× sprite pedestal + dossier + rail, ring/click sync, 94fps). Wave-3 extras: SkillTree fixed INSPECTOR w/ NEXT-UP suggestion; Online presence cards + rank diamonds (Z/X are the real binds, not </>); popups dock-tier family + MenuPopup AddSkew API fix + rank-up motes never-animated fix + Help controls copy updated. OPEN QUESTIONS for user: (1) Feedback panel says "Gold & Gems" — gems exist in code (Tamer.Gems) but guiding-star says no gems: needs ruling; (2) ~5% accent-tinted panel backgrounds offered, not decided; (3) chat POPUP now partly redundant vs the in-phone chat app (T toggles popup; phone tap opens the app) — consolidation candidate. Session ops: agents die on window close (resume via SendMessage w/ disk-state sync), watchdog stalls resume the same way, restart-after-agents law held again (hotload debris left the phone invisibly eating number keys until play-mode restart). ~240 commits local-only; push still pending user go-ahead.

**FULL PANEL SWEEP IN FLIGHT (2026-07-02 evening, →5bf286e):** user commissioned redoing EVERY panel ("one shot"). SHIPPED+VERIFIED: v8 infra (8af77e0: bottom bar RETIRED → skewed gold player-tag bottom-left + PawPad-miniature chip bottom-right; 96→56px reserves; brief at .claude/ui-knowledge/sweep-brief-v2.md); v9 (498d859: IN-PHONE APPS — chat/radio/effects/alerts render ON the phone w/ pl-hit router + ChatInputBox; PawPad FLATTENED (lean retired, projection constants zeroed = router/ring exact); wave 1 = Beasts styling/Expedition field-briefing (+ restored Hard Mode actually reaching StartExpedition)/Chat popup); wave 2 (5bf286e: Beastbook archive inversion, Shop gold market w/ display case, Bag satchel spine, Quests field journal — all capture-verified). BRIEF EVOLUTION (user): per-panel accent = its PawPad tile color; living violet ring = THE hover signature; angles mandate; clean-first for pixel art; hotkey freedom; EFFICIENCY-FIRST layouts (not novelty — "don't redo just to redo"); backgrounds stay dark-neutral (accents as highlights; optional ~5% hue tint + accent bg-geometry offered, not yet decided). PENDING: (a) Beasts recomposition (stage-primary inversion) FAILS razor codegen `} expected` — bisect-proven to its MARKUP half; agent resumed w/ incremental-reconstruction plan; broken copy at session scratchpad roster-current.razor; HEAD roster on disk meanwhile; (b) wave 3 agents resumed: SkillTree (pink), Online hub (blue, NOT guild/arena), system-popups dock-tier family (Profile/Help/Feedback/MenuPopup, violet-on-dark); (c) after: verify wave 3, commit, patchnotes exist per-panel already, learnings. Agents DIE when the chat window closes — resume via SendMessage(agentId) with disk-state sync; watchdog stalls resume the same way.

**PAWPAD v7.2–7.4 (2026-07-02 PM, →59e66b8 + learnings):** MOUSE fully working (user-verified): backdrop click router w/ InverseLean + per-frame hover poll + last-input-wins; flat-z hit/paint ordering → explicit ladder (backdrop 9600/wrap 9700/cursor 9750/swipe 9800). Wallpaper = in-flow negative-margin img (absolutes near the holder render UNLEANED), 506×702 user-tuned, graphite baked into corners (lossy WebP drops alpha in-engine). Statics = lossless WebP (PNG decode dulls blends). Nubs left:546 measured. PENDING NEXT SESSION: (1) verify wallpaper corner curves after play-mode restart (same-path texture cache — reimport doesn't reach live sessions); (2) flip WALK_DEBUG=false in PhoneLauncher once user confirms no click-bleed to panels behind; (3) user's "in-phone apps" idea (chat/radio/effects/alerts render ON the phone screen = TamerLink OS apps milestone — current grid/dock split already correct for it); (4) bottom-bar retirement, then panel sweep. User got fatigued on case pixel-tuning — prefer their live screenshot + one-number hotload iterations over my drive loop when they're at the machine.

**PAWPAD v7 "3a GRAPHITE" SHIPPED+VERIFIED (2026-07-02, →3a439c4):** the REAL design handoff finally arrived (zip 3 of 3 — first two were icons-only; ALWAYS list zip entries) with a full README spec. Live at 87fps, capture matches mockup: gold-trim graphite shell r54, bezel speaker+gold camera, gold power key, chin paw deboss (stacked recolored svg imgs), wallpaper BAKED to PNG via Playwright (tools/convert-pawpad-icons.py sibling script; animating in-place = 7fps law), gold status group w/ battery (right-anchored computed margin, name-only), no title header, 88px glyphs, non-italic 10px labels, 48px flat dock. Animated hover icons LIVE at 60fps (SRC value-swap on focus; playback user-witnessed; texture animation does NOT trigger the recomposite crater). Hover-brighten removed (user). NEW LAWS (learnings v6/v7): razor generator breaks on $-interpolation in attributes AND @-directives inside HTML comments; compile SUCCESS is silent in the log (failures only); OneDrive serves stale mid-edit snapshots to the compiler; complex wallpapers → bake to texture. REMAINING: bottom-bar retirement decision, then panel sweep Bag→QuestLog→Roster→Beastbook→Shop→SkillTree→Online; ~230 commits local-only (push standing rec).

**PAWPAD v5 SHIPPED+VERIFIED (2026-07-02, →72e609c):** Claude Design 16-icon set live (statics Assets/ui/icons/pawpad/, animated staged animated/pawpad/ PENDING WebP-in-lean fps test); System Dock (Profile/Settings/Guide/Feedback, kb 12-15, Slot0=Profile, MarkRouted plugs same-frame Enter→chat leak — leak itself not re-verified); ring = ProjectLean w/ SLOPE-FITTED constants (LeanSin 0.200 ≈ 11.5° effective vs 13° authored; measured 12/14px = centered); all-tiles accent offset shadows (value-swap law: s&box never clears a removed inline style prop); launch shoot-left ease-out; wave rebalanced + breathing; keycaps removed. NEW LAWS in learnings v5: Box.Rect = LAYOUT space; calibrate-don't-derive; inline-style-removal ghost; partial-hotload (markup fresh + old method body). SHELL redesign still OPEN — icons zip had no shell HTML; user has my Claude Design shell prompt. Old v4.2 entry below superseded.

**PAWPAD v4.2 (2026-07-02, →29e3d14, PENDING IN-ENGINE VERIFY):** user's 6-note batch executed: (1) living ring moved to ROOT level outside the lean — menu placement — restoring the per-frame liquid wave (legal out there) AND fixing left/middle column mis-centering (projected Box.Rect used directly; in-lean delta math drifts under non-uniform perspective compression); entrance-hold latch re-targets ring through the 0.28s slide. (2) hints row DELETED (user call). (3) avatar rebuilt on the GUILD roster recipe (plain in-flow img + explicit px + radius — absolute-50% offsets die under the lean, cb-avatar copy never painted). (4) PawPad logo: violet mini-tile + gold lucide paw-print beside wordmark. (5) Case/screen redesign → user prompting Claude Design with a prompt I wrote (24-col HTML mockup deliverable). Editor hadn't recompiled at commit time (needs focus); verify checklist: ring centered on LEFT column + wobbling, avatar paints, no hints, logo, fps still ~80.

**PAWPAD VERIFIED LIVE (2026-07-01 late, →415fba6):** v4.1 closed the loop — self-drive captures (now DPI-AWARE: SetProcessDPIAware or 150% scaling crops the right third; the "sliver off-screen" was half capture artifact) + log confirmed: 76–88fps with phone open (7fps crater dead), accent sweep end-to-end on all six tabs (SWEEP_DEBUG now false), phone fully on-screen right-anchored, hints/badges/keycaps clean. NEW LAW (learnings v4.1, supersedes v3.1/v4 alignment doctrine): **text-align is a NO-OP anywhere under a transformed ancestor even in a held px box** (inner text box collapses) — only alignment primitive under the lean is auto-width span + razor-computed margin-left (LabelPadPx/NamePadPx glyph estimates); labels + tamer name verified centered/right-aligned live. OPEN: does the 16px avatar circle paint the real Steam pic or the gradient fallback (src bound + in-DOM per log; pixel-ambiguous — ask user). NEXT: user blesses PawPad → bottom bar retirement → panel sweep Bag→QuestLog→Roster→Beastbook→Shop→SkillTree→Online. Focus-verify safeguard fired correctly once (Chrome foreground → zero captures) — keep it mandatory.

**SWEEP PHASE 0+1 SHIPPED (2026-07-01, Fable 5, commits →609e775):** var() spike ran in-engine — s&box rejects custom properties AND var() → **PATH B locked** (verdict in tokens.md; shared primitives ARE the token mechanism). Shipped: `BbTokens.cs` (C# tokens + element palette v1 two-tone rim/fill — Wind teal kept, Shadow pushed magenta off selection-violet, Electric greener than gold), `Code/UI/Primitives/` (BbButton 5-tier/3-size, BbSectionHeader, BbStamp, BbDiamond; ConfirmDialog = pilot adoption), `NavManager.cs` (hotload-safe: method groups + per-frame re-register + SafeInvoke + RoutedThisFrame same-frame-edge-trigger latch), **PhoneLauncher** (press 8: 3×4 app grid incl. widgets-as-apps, per-app accents, liquid expansion = solid circle imperative scale 240ms → route at half-cover → 180ms fade, 420ms; bottom bar STAYS for A/B trial), 5 dead components deleted (−1,596 lines incl. bonus ExpeditionPopup). PENDING USER VERIFY: phone in-engine (checklist in the 609e775 agent report). NEXT: bar retirement decision → panel sweep order Bag→QuestLog→Roster→Beastbook→Shop→SkillTree→Online. Agent-stall note: API watchdog killed 3 agents this session — resume via SendMessage with a nudge (keeps context), require incremental reference-complete writes.

**SESSION 2026-07-01 — FABLE 5 MAJOR UI SWEEP prep (redesigning ALL menus: panels/tabs/floating/sub-panels).** ⚠️ **User LIKES the main menu (title screen, `MainMenu.razor`) structure — KEEP it as the reference.** What they dislike & want REINVENTED (be innovative, not reskins) is the IN-GAME menus/panels: tabs, floating widgets, sub-panels, popups — their layout & especially FORMATTING. The main menu's vocabulary (motion doctrine, angled planes, italic type, engine-safe patterns) + its compositional sense transfer to those panels ("sibling of the menu"). Fable 5 gets FULL creative license, kept OPEN-ENDED (no prescriptive layout list — don't anchor it). Decisions locked with user:
- **`guiding-star.md` rewritten as the Fable 5 sweep brief** — sourced from the actual main-menu language via recon. Names the 4 signatures (living violet ring `#9b6cff` = only alive element; angled-plane dialect 13°/10°/±7° + 45° diamonds + ±3° stamps; three-color identity violet+gold`#ffce3a`+per-dest accent; italic-900 display + vector rule + no-glow). New "What the sweep fixes" section maps the user's 5 issues (weak/sprinkled movement, no angles, messy colors, inconsistent style, player confusion) → mandates. Radii now ROUNDER (16 button/22 card/26 hero/30 slab; ring = host+10 concentric).
- **Full element palette (Wind INCLUDED) = Fable 5's to redesign as ONE cohesive 11-element set.** The ONE constant that must survive: the **two-tone dark-fill + bright-rim badge** system (dark cards, element in badge only). Wind is currently teal (`#0d9488`/`#2dd4bf`) — a fine anchor but NO LONGER locked (user relaxed it 2026-07-01). Baseline to iterate from: `BeastiaryPanel.razor.scss:797-807` + `FilterBar.razor.scss:102-112`. Fixed the stale Wind=green `#15803d` ghost in `style-guide.md` (rendered NOWHERE — doc-only). User will hand Fable 5 a PHOTO of the main menu as a starting point — Fable 5 may redesign the menu itself too.
- **Retire the always-shown bottom command bar → single menu button opens a PHONE LAUNCHER** (grid of themed app buttons; phone = router/guide, NOT content). Signature transition: tap app → **liquid expansion** (solid scaling `<div>` circle in the app's accent color fills screen = that menu's bg; NOT radial-gradient — s&box rejects it). Each dest owns an accent token (button=expansion=bg). Enables the future **2D pixel-art map** (map + menu button, no persistent chrome). Needs a `NavManager` router to decouple nav from chrome; remove the 96px `::after` bottom reserve on every tab panel; remap number-key routing. PRESERVE: static `IsVisible`+`BuildHash()`, `UIModalState`, concentric focus ring.
- **Codebase is redesign-UNREADY structurally** (no token layer — ~12,400 hardcoded colors; no shared Button/Pill/Card/Modal/Header primitives; only FilterBar/MonsterCard/EmptyState/ConfirmDialog are real shared components). Prep priorities: (1) token layer — spike whether `var()` parses in s&box 26.06.03, else C# `BbTokens` static; (2) extract shared primitives + build NavManager; (3) capture-don't-copy as Fable 5 designs. Scope guardrails: DON'T sweep dormant **GuildPanel** (12.9k scss) / **ArenaPanel** (Guild Raids + Online Arena NOT live — see [[live-vs-dormant-features]]); verify/delete 4 zero-consumer components (StatBar, MoveSelectionPanel, SwapPanel, SkillNodeComponent). Tentpoles by size: Roster/Guild/BattleView/MainMenu. BattleView still OFF-LIMITS.

**SESSION 2026-06-09 (big one — ~35 commits to b1efb92+; see git log for the saga):** wave-loop video bg (h264 mp4 ONLY, all-intra + dup-frame-dropped + debounced C# Seek(0) loop), motion-doctrine v2 in guiding-star (FLOW/ALIVE/SNAP; surfaces solid; no glows; stroke purge done, −1159 lines dead SCSS), Persona pop w/ SelectedPop ring lockstep (sidebar 13°), PLAY detonation, deal-in entrance, splash revival (curated 40 lines), element-reactive featured card, Options doctrine port + Docked dual-context, smart Auto-Contract (PickBestOption; NegotiationStrategy fully removed), **Persona SCENE-SWAP system** (spec at `.claude/ui-knowledge/scene-swap-spec.md` + V2: full-width 1700px scenes, key caps, PlaneTween tweener — HOTLOAD-SAFE PlaneId resolution, never Func<Panel> lambdas, they die on hotload). Menu sounds: 3 attempts (synth v1/v2, Kenney CC0) ALL reverted — generic cues for now, ladder API stubs preserved.

**✅ TELEPORT BUG SOLVED (2026-06-10, epic hunt):** it was NEVER a ghost/cascade issue — the swap frame flipped `display none↔flex` on two giant always-mounted subtrees, paying a ~200-panel FIRST LAYOUT in one frame = **90–230ms stall with zero ticks; the screen froze on the last presented frame** (the "menu popping back" = a freeze-frame). The time-based backdrop tween silently lost its first ~30% during the stall ("backdrop won't load in"). Fix: scene shell extracted to `Code/UI/Components/LighthouseScene.razor` (own Panel + own differ; MainMenu keeps the state machine, drives via value params + method-group callbacks); NOTHING flips display — panels park at `left: -10000px` with warm layouts. EN ROUTE FIXES that were also real: `.entered.departing` specificity (s&box won't tie-break stacked state classes by source order), time-based tweens (fps-independent), debounced video wrap-seek. FALSE LEADS recorded: the differ "duplication" was a HOTLOAD artifact; PanelComponent NEVER fires OnAfterTreeRender (sweep was dead code). KEY ENGINE LESSONS (promoted to learnings): display-flip on big subtrees = layout stall; park-at--10000px instead; per-frame Log.Info census is the diagnostic tool of choice. CLEANUP PENDING: `SWAP_SLOWMO` → 1f, `SWAP_DEBUG` → false in MainMenu.razor after user verification; also BuildHash still carries SpriteAnimator.GlobalFrame + cursor position fields → menu re-renders every glide frame (the remaining fps story — next cheap win).

**Session ops lessons:** background agents DIE if the chat window closes (lost 2 mid-edit; rule: agents must never leave markup referencing undeclared members). After agents land menu code: RESTART the game, don't trust hotload (lambda death + field-migration defaults). OneDrive DEHYDRATED 1,624 files (blue screen, zero compiler lines) — fixed with attrib +P pin, see [[onedrive-stale-compiles]].

Mid-June-2026 UI overhaul, triggered by the s&box 26.06.03 CSS-parser rewrite breaking the UI (the line-height-multiplier change). State as of 2026-06-05:

- **Main menu** rebuilt as **Concept C "Launcher Hub"** (left sidebar nav + featured news card + roadmap/community cards) per a design handoff. New classes are `lh-*` in `MainMenu.razor(.scss)`; old menu-screen SCSS is dead but left in place (cleanup pending). Featured headline is hand-set `featured*` @code fields; LIVE pill uses live event data.
- **Menu→game transition** = a **Persona diagonal slide** (gold+purple slabs slide same direction, no cross) driven by mount-`@keyframes`; logo flex-centered, aspect-matched box.
- **Wind element = TEAL** game-wide (`#2dd4bf`/`#0d9488`/`#5eead4`), reversing the older slate-gray normalization (user prefers teal).
- **Animation juice** done on ~12 panels (batches 1+2: Expedition/Daily/Breeding/Achievement/Contract + Profile/Arena/Beastiary/Trading/CardCollection) and the menu.

**All of this is committed LOCALLY on `main` but UNPUSHED** — `origin/main` was at `ad6603f` (the last pushed state: line-height fix + reboot + Duodew + animation batches). Push to players only AFTER live verification on the user's machine. The earlier player-critical fixes (line-height px conversion, save reboot) ARE pushed/live.

**Canonical style spec:** `.claude/ui-knowledge/guiding-star.md` (reconciled tokens — Wind teal, currency Gold/Ink/Tokens, rarity incl. Mythic — plus the s&box CSS translation layer).

**All-panel consistency + juice pass — agreed approach (2026-06-05):** go **PANEL BY PANEL, user picks each one** (NOT a big batch workflow). **BattleView / the battle HUD is OFF-LIMITS — do NOT touch it** (bespoke in-engine surface; the user explicitly excluded it; the guide also treats battle as "intent, not spec"). Target each chosen panel to `guiding-star.md`.

Queued safe cleanups: CS0618 obsolete-API migration (`GameNetworkSystem.IsActive`→`Networking.IsActive`, `Cookie`→`Game.Cookies`, `Texture.Load`→`LoadFromFileSystem`) and the `GuildManager: Failed to load from API` null-ref. See [[onedrive-stale-compiles]].

**Recurring s&box layout gotcha (hit 3× on the Concept C menu):** "vertical / char-per-line text" = a flex width collapse. Causes seen: (1) a `transform` on a flex ancestor poisons children's width → use opacity-only entrances; (2) a container with an explicit width but **no `flex-direction: column`** row-flows its children (s&box has no display:block; undirected flex = ROW) → add `display:flex; flex-direction:column`; (3) a title mixing text nodes + a colored `<span>` becomes per-child flex items → split into explicit nowrap lines.

**2026-07-05 — Beasts page DONE (juice + angled ledger):** the My Beasts full pass landed in two commits — `6151e19` (juice foundation: moves one-column w/ inline descriptions, tooltip killed, RECORD section w/ real battle data, joined middle+right panels, centered actions, 24px XP gauge, journal modernized, legibility bump, ring last-input-wins) and `10e4763` (phase 2 "angled ledger": FIELD DOSSIER lid cap slate skewY -2°, five slates w/ wedge-straddle rooflines ±1.2° alternating lean, title chips riding rooflines, VITALS fold, lineage merged into INFO, GENE LEDGER lid in fusion mode). All 7 ranked risks verified live (top/bottom scroll, VITALS fold, fusion branch). **This page is the reference example for angling other tabs' detail columns** — the wedge-straddle recipe is in `.claude/ui-knowledge/learnings.md`. User wants ranked Arena revived eventually (own session). Facts cascade still has no audio cue (page-settle swoosh candidate).

**2026-07-05 PM — Claude Design loop + PADDING-BOX discovery:** the user now iterates via Claude Design mocks (Downloads/Detail Sidebar.dc*.html) and approves options by ID — this loop converges in ONE pass vs 3 invented iterations lost to taste. Shipped from mocks (commits 696c783 + bc90941): 1B "Signature edge" facts column; **s&box box model corrected = PADDING-BOX** (declared size includes padding, borders add; the "content-box" law was border-only evidence — CLAUDE.md row rewritten); full Exo2 weight ladder loaded (Medium/SemiBold/ExtraBold/Black were undiscovered in a subfolder — ALL small text had rendered Bold; sweep candidate for every panel); stat bar scale 550 (real ceiling: Manehelm ATK 418/515); BbHeaderWaves primitive (per-accent baked strips, seam = native-size tiles + integer geometry + exact-period wrap; fusion swaps pink); element dais baked PNGs (CSS CANNOT ellipse — border-radius 50% pills out; bake re-strengthened 0.43 peak, pending texture-cache-refresh verify); milestone journal = facts-column sub-view (13a starlight header; ranked/milestone/boss-medallion/rename/evolution/BEGINNING registers; NameChange+Ranked enum members + rename/max-level write hooks in Core); 13b/c ambient header bands; learn-move arm-then-confirm; phone 8A (GameHUD .hud-phone-btn); reactivity pass (cascade view swaps, dais flare, press states, swoosh hooks). Detail-zone liquid ring DISABLED (DetailRingEnabled=false — user: ring = grid selection only). PENDING USER: play-mode restart to verify dais texture reload + journal Q-close.

**2026-07-10 close-out:** the marathon ended with everything user-confirmed ("fixed!"). Final state: My Beasts + Contract Fusion are the flagship surfaces — 1B sidebar, milestone journal, one-cursor input model (hover selects, uniform violet cursor, Q backs out of everything), Roblox hover polarity + eased 3px lifts, inline fusion result (ritual animation rebuilt; centering + ledger layout fixed but UNVERIFIED live — needs the next real fuse), PawPad fully re-derived on padding-box + wallpaper rebaked at true geometry (rebake-wallpaper.py preserved). All committed locally through 87208ec (~260 unpushed commits; push still pending user go-ahead). Workflow now runs through the official editor MCP: camera_screenshot at native 1920 (silent) for state, engine screenshot+crop for pixel work (warn first - shutter), compile_status/read_console always. Next session candidates: fusion e2e verify, rolling the Exo2 weight ladder + 550 stat scale + one-cursor model out to the other panels, ranked Arena revival (dedicated session), oz_mcp legacy deletion.

**2026-07-12 PM — FUSION SYSTEM + FIVE-AGENT MEGA-SESSION (all landed, compile green, UNPUSHED):**
- **Cross-species fusion SHIPPED** ("Pattern Book" → user-simplified to FUSIONS): `Code/Core/FusionPatterns.cs` — hand-authored pairs, HARD RULE validator-enforced (result must be standalone; never a level-reachable evolution line; `FusionOnly` species flag scaffolded for future loom-exclusive lines). 3 launch fusions: pagefin×dewdrop→heartwell, gnoll×jackacabra→twincoil, gnoll×threadlet→jackacabra (user wove all 3). Right column = 2-wide SQUARE TILE GRID (user design): Beastbook-gated reveal (seen=sprite+name+live fuse Lv, unseen=silhouette+???), recipe strip, select-a-tile → eligible parents spotlight pink-wave in grid (user's favorite interaction). Discovery persisted (save), woven ✓. Retired beasts (#000s) are RESERVED for future content — user vetoed using them as fusion results.
- **Loom-Helix ritual**: root-caused "animation doesn't work" → **THE ANIMATION-DELAY LAW** (delayed animations are killed by re-renders; staggers = keyframe percentages of full-length t=0 animations — CLAUDE.md row). Baked rotating double-helix webps (11 elements × forge strand, `bake-helix.py`), full-column 560×800 staging, reveal fr-* family also converted + juiced (stamp slam, ledger row ticks, count-up total).
- **APP-COLOR LAW** (user): a page wears its PawPad tile color — SKILLS #ff6bd6 pink, BEASTS/fusion violet (#7b4ddb fills/#9b6cff strokes), BEASTBOOK #2dd4bf teal. Registered in design-system tokens.
- **Skill Tree redesigned** (gold→app pink; full climbable paths, silhouette locks, rank pips, earned sidebar w/ next-rank delta + branch totals, STATS category bands) + **hold-F reset** (0.9s, per-frame pixel-width fill — `Length.Percent` on absolute child paints FULL width, CLAUDE.md row).
- **INPUT GRAMMAR settled** (user-driven): WASD + walkup seams (grid→filter bar, nodes→view tabs), Space/**E** confirm (UiInput.ConfirmPressed helper), Q back, **R = cycle/rotate views or the page's power action**, Z/X = universal silent cycle pair (CyclePrev/CycleNext), M phone. **ACCEPTSFOCUS LAW**: `AcceptsFocus=true` on a page panel kills Input.Pressed game-wide after a click (the skill-tree dead-keybinds bug; CLAUDE.md row). Seam landings resume remembered position; kb scroll-follow glides everywhere.
- **Notifications → PawPad**: toasts DELETED as a category; everything routes to the phone ALERTS app (lossless history, trustworthy badge, CLEAR ALL) + transient "peeks" slide out of the paw chip corner (max 3, click opens phone). N opens phone ALERTS. Interactive popups untouched.
- **Beastbook = THE SPECIMEN HALL**: teal identity (wave-teal bake), field-specimen disc (dais-family glass+ticks — user vetoed antique brass mid-build: "museum is the CONCEPT, Beastborne is the MATERIAL"), skewed slab registry chip, mastery = bronze/silver/gold medals (rarity colors removed from mastery), corner-cut deep views, T3 discovery reveal delay-purged (was silently truncated), hover-select one-cursor.
- **PENDING USER HANDS** (MCP can't press keys): M-recovery on skills, R rotate, fusion tile 2D nav, walkup, E-confirm, hold-F amber sweep (percent→pixel fix landed after last capture). **TEMP dev commands to remove after approval**: dev_skillspage, dev_notify, dev_alerts, dev_beastbook (dev_fusefx + dev_patternbook + dev_givebeast are keepers). Stale "< >" hint chips on Shop/Online (cycle is Z/X) — sweep candidate. Delay-law blast radius narrower than "any re-render" (some live delay ladders survive) — open question in learnings.

**2026-07-12 - DESIGN SYNC LOOP ESTABLISHED:** "Beastborne UI" design-system project on claude.ai (projectId f954e318-a60d-435a-8499-77a134312786), populated via DesignSync with the 17-card live-HTML bundle at `.claude/design-system/` (authored from shipped code, anchored on guiding-star/tokens/feel-principles/style-guide; every card has @dsCard marker + ENGINE NOTES mapping browser CSS to in-engine mechanisms). THE LOOP: user designs in Claude Design with this system attached to a canvas (their "Detail Sidebar" canvas = the sketchpad) -> exports .dc.html -> we implement with zero vocabulary drift. Keep the bundle in sync when the design language changes (finalize_plan -> write_files, incremental). 5 spec-vs-code discrepancies recorded in learnings for a doc reconciliation pass (font cuts 2-vs-6, dual motion token generations, fragmented element palette, corner-cut dialect missing from guiding-star, gold-vs-pink fusion primary).
